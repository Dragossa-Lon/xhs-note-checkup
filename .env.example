# ============================================
# 笔记体检 Agent — 环境变量配置
# ============================================
#
# 复制本文件为 .env 后填入你的真实密钥：
#   cp .env.example .env
#
# 注意：.env 文件已被 .gitignore 忽略，不会提交到仓库。
# --------------------------------------------

# 【必填】DeepSeek API Key
# 申请地址：https://platform.deepseek.com/api_keys
# 国内可直连，不需要代理；最低充值 ¥10
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 【可选】使用的 DeepSeek 模型，默认 deepseek-chat
# 可选：
# - deepseek-chat   非思考模式（最便宜、速度快、适合本场景）
# - deepseek-reasoner  思考模式（更深推理、慢、贵）
# DEEPSEEK_MODEL=deepseek-chat

# 【可选】服务端口，默认 5000
# ⚠️ macOS 上 5000 被 AirPlay Receiver 默认占用，如启动报 EADDRINUSE 请改用其他端口
# PORT=5173

# 【可选】监听地址，默认 localhost（仅本机）
# 如需在容器 / LAN 里暴露，设为 0.0.0.0
# HOST=0.0.0.0

# 【可选】HTTP / SOCKS 代理（DeepSeek 国内可直连，通常不需要）
# 如果你的网络环境必须走代理才能访问外网 API，可启用：
# HTTPS_PROXY=http://127.0.0.1:7890
# ALL_PROXY=socks5://127.0.0.1:1080
